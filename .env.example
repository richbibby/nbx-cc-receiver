# Catalyst Center
CC_HOST=https://your-catalyst-center-host
CC_USER=your-username
CC_PASS=your-password

# Webhook secret (must match NetBox Cloud webhook Secret)
NB_SECRET=your-webhook-secret

# TLS verification (set to false only if CC uses self-signed and you don't have proper CA trust)
VERIFY_TLS=false

# Deployment mode for PUT (Deploy or Preview)
DEPLOYMENT_MODE=Deploy

# Path selector: generic (PUT /interface/{id}) or wireless (PUT /wirelessSettings/interfaces/{id})
INTERFACE_PATH=generic
